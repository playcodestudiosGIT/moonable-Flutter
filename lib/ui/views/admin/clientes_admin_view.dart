import 'package:flutter/material.dart';

class ClientesAdminView extends StatelessWidget {
  const ClientesAdminView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Padding(
        padding: EdgeInsets.symmetric(horizontal: 30.0),
        child: Text('Clientes'),
      ),
    );
  }
}


